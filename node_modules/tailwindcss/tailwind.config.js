/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.html"],
    theme: {
      extend: {},
    },
    plugins: [],
  }
// This is a Tailwind CSS configuration file that specifies the content files to scan for class names,
// extends the default theme, and includes no additional plugins. It is used to customize the Tailwind CSS framework
// for a project, allowing for the use of utility-first CSS classes in the HTML files listed in the content array.